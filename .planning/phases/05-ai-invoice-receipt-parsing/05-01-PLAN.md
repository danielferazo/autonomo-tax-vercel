---
phase: "05"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements:
  - INV-01
  - EXP-01
  - FILE-02
user_setup: []

must_haves:
  truths:
    - "User can upload invoice PDF/image and AI extracts all fields (number, date, client, gross, IVA, IRPF, currency)"
    - "User can upload receipt and AI extracts all fields (description, gross, IVA, category)"
    - "User can fall back to manual entry when AI parsing fails"
    - "Loading state shown during AI API call"
    - "Error handling with retry option when AI call fails"
  artifacts:
    - path: "src/lib/ai.ts"
      provides: "Claude API client for invoice/receipt parsing"
      contains: "parseInvoice, parseExpense, ANTHROPIC_API_URL, MODEL, ParsedInvoice, ParsedExpense"
    - path: "src/pages/InvoicesPage.tsx"
      provides: "Invoice upload flow with AI parsing"
      contains: "handleFile, parseInvoice, parsing state, ErrorBanner"
    - path: "src/pages/ExpensesPage.tsx"
      provides: "Receipt upload flow with AI parsing"
      contains: "handleFile, parseExpense, parsing state, ErrorBanner"
    - path: ".env.example"
      provides: "VITE_ANTHROPIC_API_KEY placeholder"
      contains: "VITE_ANTHROPIC_API_KEY"
  key_links:
    - from: "src/pages/InvoicesPage.tsx"
      to: "src/lib/ai.ts"
      via: "import { parseInvoice }"
      pattern: "parseInvoice"
    - from: "src/pages/ExpensesPage.tsx"
      to: "src/lib/ai.ts"
      via: "import { parseExpense }"
      pattern: "parseExpense"
---

<objective>
Confirm AI Invoice/Receipt Parsing (INV-01, EXP-01, FILE-02) is fully implemented across the codebase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@src/lib/ai.ts
@src/pages/InvoicesPage.tsx
@src/pages/ExpensesPage.tsx
@src/components/ui/FileDropzone.tsx
@src/components/ui/ErrorBanner.tsx
@.env.example
@src/types/database.ts
</context>

<intentions>
This plan performs verification only. All three requirements are already implemented:

- **INV-01**: `InvoicesPage` imports `parseInvoice` from `ai.ts`, `handleFile` calls it, `FileDropzone` passes file, form populates with AI-extracted fields. Manual fallback present via form fields remaining editable.
- **EXP-01**: `ExpensesPage` imports `parseExpense` from `ai.ts`, `handleFile` calls it, `FileDropzone` passes file, form populates with AI-extracted fields. Manual fallback present via form fields remaining editable.
- **FILE-02**: `src/lib/ai.ts` contains direct `fetch` calls to `https://api.anthropic.com/v1/messages` using `VITE_ANTHROPIC_API_KEY` env var. No edge function needed.

No code changes required. Execute-phase will verify each artifact and criterion.
</intentions>

<tasks>

<task type="auto">
  <name>Task 1: Verify ai.ts implementation</name>
  <files>src/lib/ai.ts</files>
  <action>
Read src/lib/ai.ts. Verify it exports:
- `parseInvoice(fileData: string): Promise<ParsedInvoice>` — calls Anthropic API with invoice system prompt, returns number, date, client, currency, gross_orig, iva_collected, irpf_retained
- `parseExpense(fileData: string): Promise<ParsedExpense>` — calls Anthropic API with expense system prompt, returns description, gross, iva_paid, category
- Both use `import.meta.env.VITE_ANTHROPIC_API_KEY` (not hardcoded)
- Both use model `claude-sonnet-4-6-20250514`
- No console.log statements
  </action>
  <verify>
grep -n "export async function parseInvoice\|export async function parseExpense\|VITE_ANTHROPIC_API_KEY\|MODEL\|console\.log" src/lib/ai.ts
grep -n "x-api-key\|anthropic-version\|content-type" src/lib/ai.ts
  </verify>
  <done>ai.ts confirmed with parseInvoice, parseExpense, direct Anthropic API calls, no console.log</done>
</task>

<task type="auto">
  <name>Task 2: Verify InvoicesPage AI integration</name>
  <files>src/pages/InvoicesPage.tsx</files>
  <action>
Read src/pages/InvoicesPage.tsx. Verify:
- Imports parseInvoice from ../lib/ai
- Has parsing state (useState(false)) and parseError state (useState(null))
- handleFile reads file as base64 via fileToBase64, calls parseInvoice(base64), populates form with parsed fields (number, date, client, currency, gross_orig, iva_collected, irpf_retained)
- FileDropzone receives parsing={parsing} and error={parseError}
- ErrorBanner shown for parseError with onDismiss
- catch block sets parseError with user-friendly message
- Manual entry form remains fully functional (all fields editable after AI populate)
  </action>
  <verify>
grep -n "parseInvoice\|parsing\|parseError\|ErrorBanner" src/pages/InvoicesPage.tsx
grep -n "handleFile" src/pages/InvoicesPage.tsx
  </verify>
  <done>InvoicesPage AI upload flow verified: parsing state, ErrorBanner, handleFile with parseInvoice call, manual fallback</done>
</task>

<task type="auto">
  <name>Task 3: Verify ExpensesPage AI integration</name>
  <files>src/pages/ExpensesPage.tsx</files>
  <action>
Read src/pages/ExpensesPage.tsx. Verify:
- Imports parseExpense from ../lib/ai
- Has parsing state (useState(false)) and parseError state (useState(null))
- handleFile reads file as base64 via fileToBase64, calls parseExpense(base64), populates form with parsed fields (description, gross, iva_paid, category)
- FileDropzone receives parsing={parsing} and error={parseError}
- ErrorBanner shown for parseError with onDismiss
- catch block sets parseError with user-friendly message
- Manual entry form remains fully functional (all fields editable after AI populate)
  </action>
  <verify>
grep -n "parseExpense\|parsing\|parseError\|ErrorBanner" src/pages/ExpensesPage.tsx
grep -n "handleFile" src/pages/ExpensesPage.tsx
  </verify>
  <done>ExpensesPage AI upload flow verified: parsing state, ErrorBanner, handleFile with parseExpense call, manual fallback</done>
</task>

<task type="auto">
  <name>Task 4: Verify env configuration</name>
  <files>.env.example</files>
  <action>
Read .env.example. Verify it contains:
- VITE_ANTHROPIC_API_KEY with a placeholder sk-ant-... value
- Comment explaining it is for Phase 2+ (AI parsing)
  </action>
  <verify>
grep -n "ANTHROPIC\|VITE_ANTHROPIC_API_KEY" .env.example
  </verify>
  <done>.env.example confirmed with VITE_ANTHROPIC_API_KEY placeholder</done>
</task>

<task type="auto">
  <name>Task 5: Verify TypeScript compiles cleanly</name>
  <files>src/lib/ai.ts, src/pages/InvoicesPage.tsx, src/pages/ExpensesPage.tsx</files>
  <action>
Run tsc --noEmit to confirm no type errors in ai.ts or the two modified pages.
  </action>
  <verify>
node_modules/.bin/tsc --noEmit 2>&1 | head -20; echo "Exit code: $?"
  </verify>
  <done>TypeScript compilation passes with no errors</done>
</task>

<task type="auto">
  <name>Task 6: Verify no console.log in ai.ts</name>
  <files>src/lib/ai.ts</files>
  <action>
Verify ai.ts contains zero console.log statements.
  </action>
  <verify>
grep -n "console\.log" src/lib/ai.ts; test $? -eq 1 && echo "PASS: no console.log found"
  </verify>
  <done>No console.log statements in ai.ts</done>
</task>

</tasks>

<verification>
## Phase 5 Verification Checklist

| Requirement | Criterion | Status |
|-------------|-----------|--------|
| INV-01 | parseInvoice imported and called in InvoicesPage handleFile | verify grep |
| INV-01 | FileDropzone receives parsing + error props | verify grep |
| INV-01 | ErrorBanner renders for parseError | verify grep |
| INV-01 | All form fields remain editable after AI populate | verify code |
| EXP-01 | parseExpense imported and called in ExpensesPage handleFile | verify grep |
| EXP-01 | FileDropzone receives parsing + error props | verify grep |
| EXP-01 | ErrorBanner renders for parseError | verify grep |
| EXP-01 | All form fields remain editable after AI populate | verify code |
| FILE-02 | ai.ts calls Anthropic API directly (not via edge function) | verify code |
| FILE-02 | Uses VITE_ANTHROPIC_API_KEY from env, not hardcoded | verify code |
| General | tsc --noEmit passes | verify command |
| General | No console.log in ai.ts | verify grep |
| General | .env.example has VITE_ANTHROPIC_API_KEY | verify grep |
</verification>

<success_criteria>
All 6 verification tasks pass:
1. ai.ts exports parseInvoice + parseExpense with correct signatures
2. InvoicesPage AI integration verified
3. ExpensesPage AI integration verified
4. .env.example has VITE_ANTHROPIC_API_KEY
5. tsc --noEmit passes
6. No console.log in ai.ts
</success_criteria>

<output>
After completion, create `.planning/phases/05-ai-invoice-receipt-parsing/05-01-SUMMARY.md`
</output>
